set(
  SUPPORTED_DRIVERS "lincan;peak_linux;serial;socket;virtual"
)

set(
  DRIVER "socket"
  CACHE STRING "The CAN driver to use. The following drivers are available: ${SUPPORTED_DRIVERS}."
)

set(
  BUILD_DRIVERS ""
  CACHE STRING "Semicolon-separated list of additional CAN drivers to build. The following drivers are available: ${SUPPORTED_DRIVERS}."
)

option(
  BUILD_ALL_DRIVERS "Build all available drivers." OFF
)

option(
  EXHAUSTIVE_DEBUGGING "Enable exhaustive debugging." OFF
)
